let hasWallet = true ;
let hasBalance = false ; 

console.log(hasWallet && hasBalance);
console.log(hasWallet || hasBalance);
console.log(!hasWallet);

let age = 20 ;
let result = age >= 18 ? "Adult":"Minor";
console.log(result);
